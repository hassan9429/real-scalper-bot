[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "python3 bot.py"
```

4. Click **Commit changes**

---

## Also Update Procfile:

Click `Procfile` → edit → change to:
```
web: python3 bot.py
